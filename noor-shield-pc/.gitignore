node_modules/
dist/
out/
*.log
.DS_Store

# Plaintext product keys from scripts/generate-keys.js — only their hashes
# (src/main/licenseKeyHashes.json) are ever meant to be committed.
new-product-keys*.txt
all-*-product-keys.txt

# Generated by node-windows when the service is installed (wrapper exe, XML
# config, WinSW logs) — machine-specific, never checked in.
service/daemon/
